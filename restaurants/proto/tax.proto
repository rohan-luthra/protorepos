syntax = "proto3";

package restaurants;

enum OrderType {
    DINEIN = 0;
    TAKEAWAY = 1;
    AGGREGATORS = 2;
    QSR = 3;
}


message Tax {
    string _id = 1;
    string name = 2;
    bool inclusice = 3;
    bool is_percentage = 4;
    float value = 5;
}